{% extends 'SIEBundle::main.html.twig' %}
{% block title %}Listado de Proveedores {% endblock %}
{% block contenido %}
	<article>
		<h1>Listado de Proveedores</h1>
		<table class="table table-striped">
			<tr><th>ID</th>
				<th>Razon Social</th>
				<th>RFC</th>
				<th>Dirección</th>
				<th>Telefono</th>
				<th>Editar</th>
				<th>Eliminar</th>
			</tr>
			{% for proveedor in proveedor %}
				<tr>
					<td><a href="{{url('proveedores_detalle',{'id':proveedor.id})}}">{{proveedor.id}} </a></td>
					<td><a href="{{url('proveedores_detalle',{'id':proveedor.id})}}">{{proveedor.razonsocial}}</a></td>
					<td><a href="{{url('proveedores_detalle',{'id':proveedor.id})}}">{{proveedor.RFC}}</a></td>
					<td><a href="{{url('proveedores_detalle',{'id':proveedor.id})}}">{{proveedor.direccion}}</a></td>
					<td><a href="{{url('proveedores_detalle',{'id':proveedor.id})}}">{{proveedor.telefono}}</a></td>
					<td><a href="{{url('proveedores_edit',{'id':proveedor.id})}}">Editar</a></td>
					<td><a href="{{url('proveedores_delete',{'id':proveedor.id})}}">Eliminar</a></td>
				</tr>
			{% endfor %}
		</table>
		<input class="btn btn-default" type="submit" value="Nuevo" onclick="window.location='{{path('proveedores_nuevo')}}'">
	</article>
{% endblock %}