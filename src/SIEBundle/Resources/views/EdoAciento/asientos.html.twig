{% extends 'SIEBundle::main.html.twig' %}
{% block title %}Tipo de Estados de Asientos{% endblock %}
{% block contenido %}
<article>
	<h1>Tipos de Asientos</h1>
	<table class="header">
		<tr>
			<th>ID</th>
			<th>Tipo de Estados de Asiento</h>
			<th>Editar</th>
			<th>Eliminar</th>
		</tr>
		{% for edo in edo %}
			<tr>
				<td>{{edo.id}}</td>
				<td>{{edo.estado}}</td>
				<td><a href="{{url('edo_aciento_edit',{'id': edo.id})}}">Editar</a></td>
				<td><a href="{{url('edo_aciento_delete',{'id': edo.id})}}">Eliminar</a></td>
			</tr>
		{% endfor %}
	</table>
	<input type="submit" value="Nuevo" onclick="window.location='{{path('edo_aciento_new')}}'">
</article>
{% endblock %}